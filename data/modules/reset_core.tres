[gd_resource type="Resource" script_class="ModuleData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/module_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = &"reset_core"
name = "Reset Core"
rarity = &"epic"
effect_key = &"first_use_cd_zero"
values = {}
description = "1/battle: CD 0."
detail_description = "Once per battle, after this slot's card is used, its cooldown becomes 0."
