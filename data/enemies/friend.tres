[gd_resource type="Resource" script_class="EnemyData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/enemy_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = &"friend"
name = "Friend"
color = &"light_gray"
initial = "F"
rank = &"normal"
category = &"support"
hp = 30
attack = 2
move_pattern_id = &"straight_1"
traits = Array[StringName]([&"attach_support"])
danger_score = 6
keywords = PackedStringArray("Friend", "Attach")
description = "Attaches on same tile, or seeks the nearest ally from turn 3. While attached, it cannot be targeted by single-target attacks and grants +2 attack, +1 speed."
